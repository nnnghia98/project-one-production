export interface IFlipCardProps {
  title: string;
  cover: string;
  detail: string;
}

export interface IProjectProps {
  bannerSrc?: string;
  name: string;
  shortDes: string;
  repoUrl?: string;
  websiteUrl?: string;
}
