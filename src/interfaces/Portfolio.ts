export interface IFlipCardProps {
  cover: string;
  detail: string;
}

export interface IProjectProps {
  bannerSrc?: string;
  name: string;
  shortDes: string;
  repoUrl?: string;
  websiteUrl?: string;
}
